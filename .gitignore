# Python
__pycache__/
*.py[cod]
*.so

# Data
data/*.npz
!data/omnifall_preprocessed.npz
!data/omnifall_pose.npz
data/*.tar.gz
data/*.csv
data/cifar-10-batches-py/

# 逐视频 .pt 特征目录（体积大，不入库；汇总见 PROJECT_CONTENTS.md「data/」）
data/features/
data/dinov2_features/
data/dinov2_giant_features/
data/pose_features/
data/optical_flow_features/

# E3 帧级微调缓存与重抽特征
data/p9_frames/
data/p9_features_finetuned/

# E1 连续帧缓存（训练 + val 监控）
data/p9e1_frames/
data/p9e1_frames_val/

# Phase 10 全画面抽帧缓存（23 GB / 97 万文件；experiments/prep_p10_frames.py 可重生成）
data/ref_frames/
data/.frames_geometry_squash

# 预训练权重（可重新下载；*.pth 不在 LFS 规则里，入库会变成 93 MB 普通对象）
data/*.pth

# 参考基线的 checkpoint 只保留在本地磁盘（约 5 GB，各阶段 runner 可重训）：
#   phase 10 全部模型 + 本次回填的 phase 6 运行 + phase 9 的其余实验；
#   主模型 E1-full 的 checkpoint 已于 Phase 9 收官走 LFS 入库（tracked 文件不受本规则影响）。
# 若要像 phase1–8 那样把它们也走 LFS 入库，删掉下面几行再 `git add -f` 即可。
logs/phase9/**/*.pt
logs/phase10/**/*.pt
logs/phase6/p6d_bt_*/**/*.pt
logs/phase6/p6e_bilstm_*/**/*.pt

# Dataset
DATASET-omnifall/

# Diagnostic output (large, regenerable)
analysis/diagnose_lying_event/

# IDE
.vscode/

# Claude
.claude/

# Jupyter
.ipynb_checkpoints/

# Root-level archives (too large for GitHub)
/*.tar.gz

# OS
.DS_Store
Thumbs.db

# 第三方参考实现（vendored，约 1.1GB：C3D/SlowFast/TimeSformer/kinetics-i3d/scenic/TSM）
reference/

# Agent 技能脚手架（同 .claude/）
.agents/
AGENTS.md

# E1-full 本地日志镜像（内容与 run.log / eval_dump.log 重复）
logs/phase9/e1_full/*.live.log
