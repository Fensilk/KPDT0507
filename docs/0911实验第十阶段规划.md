# 第十阶段规划草案：参考视频模型在 OmniFall 上的适配与对比

> 日期：2026-09-11
> 状态：规划（Plan），待确认口径与预算后进入开发
> 本文件 = 阶段规划 + 六个参考仓库的逐个核查结论 + 适配方案

---

## 1. 目标与非目标

### 1.1 目标

1. 把 `reference/` 下的 6 个公开视频模型（C3D / I3D / SlowFast+X3D / TSM / TimeSformer / ViViT）
   适配到 OmniFall OF-Syn，**用同一数据划分、同一评估口径**跑出可比基线。
2. 回答论文里必须回答的问题：**通用视频动作识别模型 vs 本项目的"跌倒专用时序建模"，
   差距在哪里？** 特别是最难的 fall / fallen / lying（静态轴）三分类。
3. 产出可直接进论文的对比表 + 定性失败案例（谁是"把 lying 判成 fallen"的元凶）。

### 1.2 非目标（本阶段不做）

- 不追求复现各模型在原论文数据集（Kinetics/UCF101）上的精度。
- 不做 16 类细粒度任务，只做三元（fall / fallen / normal）。
- 不重训 ImageNet/Kinetics 预训练权重（成本不划算，且与公平性无关）。
- 不把 6 个模型都调到各自最优；目标是"代表性配置 + 统一协议"。

---

## 2. 现状基线（Phase 9）

**Phase 9 已结束，参考模型要对比的基线 = E1-full（终值）**，来源 `E1-report.html`。

主模型：DINOv2-giant（冻结主干 + LoRA r16，可训 10.82 M）+ Phase6 bridge 时序头；
全量 train 9,600 视频，3 epochs / 28,800 步 / 42.3 h；窗口 64 帧、训练 stride 16、评估 stride 8；
AdamW lr 1e-4，逐帧三元 CE + 逆频权重 [1.318, 1.545, 0.137]。

### 2.1 instance 口径（全量 test 1,200 视频 / 230,400 帧实例）

| 轴 | baseline（p7d_delta） | **E1-full best** | Δ |
|---|---|---|---|
| fall_prec | 0.5918 | **0.6951** | +0.103 |
| fall_rec | 0.7923 | **0.8305** | +0.038 |
| fallen_prec | 0.6272 | **0.7551** | +0.128 |
| fallen_rec | 0.6984 | **0.7538** | +0.055 |
| avg_f1（三类宏平均） | 0.7641 | **0.8268** | +0.063 |
| ternary_acc | 0.9079 | **0.9367** | +0.029 |

派生 F1（由 P/R 计算，供表格引用）：fall_f1 **0.7568**、fallen_f1 **0.7544**、normal_f1 **0.9693**。

### 2.2 事件级（timeline）口径（逐视频，每视频等权）

| 指标 | baseline | **E1-full best** | Δ |
|---|---|---|---|
| fall_f1（逐视频） | 0.7291 | **0.7827** | +0.054 |
| fallen_f1（逐视频） | 0.6584 | **0.6989** | +0.041 |
| avg_f1（逐视频） | 0.7051 | **0.7413** | +0.036 |
| 覆盖率 · fall（211 视频） | 0.953 | **0.962** | +0.009 |
| 覆盖率 · fallen（150 视频） | 0.807 | **0.833** | +0.026 |
| 误报率 · fall（989 视频） | 0.088 | **0.026** | −0.062 |
| 误报率 · fallen（1,050 视频） | 0.079 | **0.035** | −0.044 |
| 翻转 / 视频（时序抖动） | 1.41 | **0.63** | −0.78 |

> 结论（写进论文的论点）：E1-full 的增益**压倒性地落在两个精度轴**（fallen_prec +0.128、
> fall_prec +0.103），召回只小幅提升；即端到端修的是"判决边界质量"，不是召回。
> 且 E1 裸模型的事件 F1（raw 0.8269）已超过 baseline 依赖其调参后处理规则得到的 0.7735
> —— 规则红利被内嵌进了网络。

### 2.3 对第十阶段的约束

- 参考模型必须**同时报两个口径**：instance（帧级，与 2.1 同表）与 timeline（事件级，与 2.2 同表）。
  事件级沿用 `utils/timeline_metrics.py`（覆盖率 / 误报率 / 逐视频 F1 / 翻转次数）。
- **本阶段不做任何后处理规则对照**（相关规则已在 Phase 9 迭代中弃用）；所有参考模型只报
  裸模型结果，与 E1-full 的裸模型结果直接对齐。

---

## 3. 资产盘点

### 3.1 数据侧（已在本机核实）

| 资产 | 路径 | 事实 |
|---|---|---|
| 原始视频 | `DATASET-omnifall/data_files/extracted/<class>/*.mp4` | 10 类 × 1200 = **12,000** 个；**1280×720 / 16 fps / 81 帧 / AV1 编码** |
| 数据划分 | `DATASET-omnifall/splits/syn/random/{train,val,test}.csv` | **9600 / 1200 / 1200**，单列 `path`（如 `fall/fall_ch_001`） |
| 逐帧标签 | `data/ofsyn_frame_labels.csv` | 每帧 16 类 label + `fall_label` / `fallen_label` |
| 片段标签 | `data/ofsyn_clips.csv` | 每片段 16 帧（=1s），含 start/end/center_frame |
| 三元映射 | `models/dataset.py`（L505-509） | 16类 1(fall)→**0=fall**；2(fallen)→**1=fallen**；其余→**2=normal**（lying 归入 normal） |
| 已抽帧 | `data/p9_frames`、`data/p9e1_frames(_val)` | 2000 / 2000 / 150 个视频；**每视频 10 帧、224×224、stride 8**（E1 专用，不足以喂 32/64 帧模型） |
| 解码工具 | `preprocessing/av_decode.py` | PyAV 解 AV1（cv2 会报 "Missing Sequence Header"，本地已验证 PyAV 可用，av 17.0.1） |

**结论**：原始视频齐全、划分齐全、标签齐全；缺的只是"高帧率密集抽帧"这一份共享中间产物。

### 3.2 代码侧：六个仓库逐个核查结论

`reference/` 为本地第三方代码库（建议加入 `.gitignore`，不入库；在文档里只记 commit 哈希）。

**引用来源（论文与官方仓库）**

| 模型 | 论文 | 官方仓库 |
|---|---|---|
| C3D | Learning Spatiotemporal Features with 3D Convolutional Networks（ICCV 2015） | https://github.com/facebook/C3D |
| I3D | Quo Vadis, Action Recognition? A New Model and the Kinetics Dataset（CVPR 2017） | https://github.com/deepmind/kinetics-i3d |
| SlowFast | SlowFast Networks for Video Recognition（ICCV 2019） | https://github.com/facebookresearch/SlowFast |
| X3D | X3D: Expanding Architectures for Efficient Video Recognition（CVPR 2020） | https://github.com/facebookresearch/SlowFast |
| TSM | Temporal Shift Module for Efficient Video Understanding（ICCV 2019） | https://github.com/mit-han-lab/temporal-shift-module |
| TimeSformer | Is Space-Time Attention All You Need for Video Understanding（ICML 2021） | https://github.com/facebookresearch/TimeSformer |
| ViViT | ViViT: A Video Vision Transformer（ICCV 2021） | https://github.com/google-research/scenic |

下表"仓库 / commit"列是本地 `reference/` 中实际核查过的克隆版本。

| 模型 | 仓库 / commit | 框架 | 原生输入 | 权重 | 适配难度 |
|---|---|---|---|---|---|
| C3D | `reference/C3D` (71336a2) | **Caffe**（C3D-v1.1 = 完整 Caffe fork） | 16×112×112 | **仓库内无 .caffemodel** | 高 |
| I3D | `reference/kinetics-i3d` (0667e88) | **TensorFlow 1 + Sonnet** | RGB 64×224×224；Flow 64×224×224×2 | **已下载 5 套（2.1GB）** | 中-高 |
| SlowFast / X3D | `reference/SlowFast` (287ec00) | PyTorch（detectron2 生态） | 视频/帧，X3D-XS: 4 帧×采样间隔12，crop 160 | Model Zoo 可下（x3d_xs/s/m/l.pyth） | 中 |
| TSM | `reference/temporal-shift-module` (1f6a834) | **PyTorch（最轻）** | 帧目录 `img_{:05d}.jpg`，8/16 帧，224 | 官网 .pth（K400 8f/16f） | **低** |
| TimeSformer | `reference/TimeSformer` (a5ef29a) | PyTorch（自带 PySlowFast fork） | 8×224×224，sampling_rate 32 | Dropbox（K400/K600/SSv2） | 中 |
| ViViT | `reference/scenic` (a179cef) | **JAX/Flax + DMVR/TFRecord** | 32×224×224 | Google Scenic checkpoint | **很高** |

逐个核查到的关键事实（决定改造点）：

- **TSM**：数据入口在 `ops/dataset_config.py` 的 `return_<dataset>(modality)`，返回
  `(num_class, train_list, val_list, root_path, prefix)`；`main.py` 直接调用它。
  → 只要新增一个 `return_omnifall()` + 两个文本列表即可跑通，是改动量最小的一个。
- **SlowFast**：`slowfast/datasets/kinetics.py` 的标注格式就是 **每行 `视频路径 标签`**，
  解码后端可切 `torchvision`/`PyAV`；`configs/Kinetics/X3D_XS.yaml` 已给出 X3D-XS 全套超参。
  → 主要工作是把 OmniFall 转成 Kinetics 风格 CSV + 改 `NUM_CLASSES`。
- **TimeSformer**：训练路径自带 `timesformer/{datasets,models,utils}`（PySlowFast 的 fork），
  **只有可视化脚本 `tools/visualization.py` 才真正 `import slowfast`**。
  → 训练不需要额外装 PySlowFast，但需要 `fvcore` / `timm` / `einops`。
  配置关键项：`NUM_FRAMES 8, SAMPLING_RATE 32, TRAIN_CROP_SIZE 224, BATCH_SIZE 8, NUM_CLASSES 400`。
- **I3D**：`i3d.py` 直接 `import sonnet`，`evaluate_sample.py` 用 `tf.placeholder`，
  是**标准 TF1 代码（需 Python≤3.7）**；权重已就位，5 套 checkpoint 文件名与官方脚本一致。
- **C3D**：仓库只有 prototxt 架构定义（`crop_size:112, new_height:128, new_width:171, batch_size:30`）
  和 `.lst` 列表，**没有任何 .caffemodel**；C3D-v1.1 是整套 Caffe 源码（需自带 CUDA8 时代工具链）。
- **ViViT**：官方数据管线要求 **DMVR + TFRecord**（`data/data.md`），JAX/Flax 训练循环，
  全部模型取 **32 帧**输入；改成 OmniFall 需要自己写 TFRecord 生成器。

---

## 4. 统一评估协议（先钉死，再动手）

参考模型都是"一个 clip → 一个标签"的分类器，而我们的主线是"每一帧一个三元标签"。
两者要对齐，必须先定义转换口径。

### 4.1 主口径：帧级三元（与 E1/E3 完全一致）

1. 每个模型吃一个窗口（clip），输出三分类 logits。
2. **滑窗推理**：stride 8（与主线一致），把 clip 预测写回其**中心帧**；
   重叠处按 logits 平均（或按 max 取 fall/fallen），得到逐帧三元预测。
3. 指标：`ternary_acc / fall_f1 / fallen_f1 / avg_f1`（instance 口径，230,400 实例），
   与 `logs/phase9/e1_full/eval_best.json` 同口径直接可比。
4. 事件级（timeline 口径，与 E1-full 报告 §03 同源）：逐视频 F1、覆盖率、误报率、翻转/视频。

### 4.2 窗口长度：两条线都报

窗口长度是模型的强先验，强行统一会削弱它们；但完全放开又不可比。因此：

| 线 | 规则 | 用途 |
|---|---|---|
| 主表 | 各模型**原生最优窗口**（TSM 8/16、TimeSformer 8、X3D-XS 4×12、I3D 64、ViViT 32） | 体现各模型真实实力 |
| 对照表 | 全部统一 **16 帧**（中心帧标签） | 排除窗口长度这一混杂因素 |

### 4.3 阶梯式评测（控制成本的关键）

每个模型分两档，先便宜的后贵的：

- **T1 线性探测**：冻结预训练骨干，抽 clip 级特征（全局池化），训练线性/MLP 头（3 类）。
  成本 ≈ 抽一遍特征 + 几分钟训练，用于回答"通用视频特征好不好用"。
- **T2 全量微调**：端到端训练（本阶段主结果）。

如果时间不够，ViViT / I3D 可以只交 T1 结果，并在论文中明确标注。

### 4.4 公平性约定

**投入上限（2026-09-13 用户定调）**：本阶段的目的是**横向对比、给我们的模型提供参照**，
不是把基线调好。因此每个参考模型**只跑一套标准配方**（官方/默认超参 + 我们的统一数据与评测口径），
**不做逐模型调参、不做架构改动、不做多 seed**。公平性以"同一数据、同一评测口径、同一标签预算"
保证，而不是以"每个模型都调到最优"保证。TSM 的公平版（§3.7）即按此定稿，不再迭代。

---

### 4.4.1 模型预算判定原则（2026-09-13 定稿，本阶段通用）

**总则**：**唯一的对标对象是 E1-full**；基线之间**不要求统一预算**。
每个模型的预算必须由**证据**选出（不是拍脑袋、也不是沿用其它模型的数字），并记录在汇总表中。

**流程**（三步）：

1. **初始探针预算 B=5**：余弦退火 `T_max=5`，跑满（LR 退火到≈0）。这一次本来就要跑，成本不浪费。
2. **看 val 峰值落在预算的哪个位置**：

   | B=5 跑完后的观察 | 推断 | 行动 |
   |---|---|---|
   | 峰值在中间（ep2–ep4） | 预算合适：高 LR 学习期跑够，退火也走完 | **保留**，进评测 |
   | 峰值在最后一轮（ep5）且此前单调上升 | 可能欠训练；但**也可能是退火效应，单条曲线无法区分** | 用 **2,000 视频子集**做 `B=5 vs B=10` 对比（约 0.6 h / 1.2 h），胜者再跑全量 |
   | 峰值在 ep1–ep2、之后明显下滑 | 预算偏长（早早过拟合） | 可缩到 3 轮；或保留 5（通常差异很小） |

3. **记录**：在总结文档中写明该模型的 `预算 / val 峰值位置 / 是否补探针 / 最终采纳理由`。

**明令禁止的判定方式**：

- ❌ 靠"val 还在涨"就直接加轮数——X3D-S 实测证明那多半是退火到零的效应，不是没学完；
- ❌ 用 early stop 掐断退火过程后仍以该结果为准——若使用 early stop，必须保证预算足够让 LR 退火
  到接近 0，或把 early stop 仅当作安全网（本次 X3D-S 的 T_max=15 版就是被 early stop 在第 6 轮掐断，
  其退火段从未发生，故该结果只能作为证据、不能作为最终成绩）；
- ❌ 跨不同预算/调度直接比较模型优劣——预算必须在报告里注明。

**已执行案例**：

| 模型 | 采纳预算 | val 峰值位置 | 是否补探针 | 结论 |
|---|---|---|---|---|
| TSM | 5 轮余弦 | ep2（中间偏前） | 否 | 预算合适 ✅ |
| X3D-S | 5 轮余弦 | ep5（末端） | 是：补 `T_max=15` 对照，best 0.7282 < 0.7474 | 5 轮为胜者 ✅ |

> 修订过程与依据（实测，X3D-S）：
> ① 首版固定 5 轮时 val 逐轮上升（0.703→0.720→0.736→0.743→**0.747**），
>    一度被误判为"训练不够、需要更多轮"；
> ② 改成 T_max=15 + early stop(3) 重跑后，最好只有 **0.7282**，且第 6 轮即早停（ep3 见顶后回落）；
> ③ 结论：**差异来自 LR 调度长度而非数据量**——T_max=5 时第 5 轮 LR 已退火到≈0，曲线自然单调上升；
>    T_max=15 时同期 LR 仍有 ≈0.0075，模型持续大幅更新，val 提前见顶后回落。
> 方法学含义（写进论文）：**跨不同 epoch/调度预算比较基线是不公平的**，
> 必须固定同一调度形状与预算；"多训几轮会不会更好"不能靠曲线形状判断。

**基线实现来源（务实路线，优先低环境成本）**

| 模型 | 实现来源 | 预训练 | 相对原论文的差异（需在文中注明） |
|---|---|---|---|
| X3D | `pytorchvideo`（与 SlowFast 仓库同源，含 X3D-S/M） | Kinetics-400 | 训练框架为轻量 PyTorch 封装，非 PySlowFast 完整训练器 |
| SlowFast | `pytorchvideo`（slowfast_r50） | Kinetics-400 | 同上 |
| TimeSformer | `reference/TimeSformer`（官方仓库，自包含 PyTorch） | K400（Dropbox 权重） | 无 |
| I3D | 优先 `pytorchvideo` 的 I3D-R50；如需论文原始版则走 DeepMind TF1 + 权重转换 | Kinetics-400 | **I3D-R50 ≠ DeepMind Inception-I3D**，两者必须区分命名 |
| C3D | 自研 PyTorch 复刻（官方仓库是 Caffe fork 且不含权重） | 从零训练 | 非官方实现，需注明参数量对齐 |
| ViViT | 暂缓（JAX/Flax + TFRecord 数据管线成本最高） | — | 若做，需单列说明 |

**公平性三轴协议（2026-09-13 定稿，适用于全部 6 个参考模型）**

| 轴 | 规则 |
|---|---|
| 模型/范式 | **谁都不改**。TSM 就是 8 帧滑窗 + 通道位移分类器；不换头、不改输出粒度、不给它加我们的时序设计。 |
| 数据/输入 | **必须对齐**：所有模型都看到**完整画面**（不做中心裁剪丢视野）。E1 的 DINOv2 输入本来就是整帧 resize，TSM 早期版本只保留 56% 画面宽度 → 已修正为全画面。 |
| 训练/预算 | **必须对齐并声明量纲**。本阶段采用"**标签实例数对齐**"：E1-full 3 epoch × 128 标签/视频 = 369 万；TSM 用 stride 1（73 clip/视频 × 9,600 = 70 万/epoch）× 5 epoch ≈ 350 万。另给参考模型**同等的调参努力**（在 2,000 视频子集上前置筛选 lr/正则，取 val 最优）。 |

**本阶段明确不做**（已知残留差异，写进论文的 limitations）：
- 两侧的逐帧合并规则不完全相同（E1 用 3 窗口多数投票，参考模型用概率平均）。实测该差异对指标影响 ≤0.003，
  而模型间差距是 0.12–0.19，故不再花 GPU 统一（统一一次约 10 min GPU，可在需要时补）。
- 不对参考模型做"冻结特征 + 线性探测"（F5）——那是另一档协议，本阶段只用主表口径。
- 不把"改变窗口长度/换成 16 帧"当作修正手段；若要做，必须作为**独立的模型行**报告。

- 划分、种子（42）、测试视频（1200）完全一致。
- **数据规模（硬规则）**：主表 = 参考模型与 **E1-full 同在 9,600 训练视频**上训练。
  Phase 9 的核心教训就是"数据量不对称的对比是无效结论"（见 `0904实验第九阶段E1小结.md` §5），
  所以**不允许**出现"参考模型用 2,000 视频 vs E1-full 用 9,600 视频"这类对比。
  2,000 视频子集（与 E1@2000 / E3@2000 严格同批，可用类别权重是否吻合来校验）只用于两件事：
  ① 工程冒烟（跑通链路、测吞吐/显存）；② 若论文需要一张"同数据受控子表"，
  则 E1@2000 / E3@2000 / 参考模型@2000 三者同表，且必须标注清楚数据量。
  **② 默认不做**（主表 9,600 已能回答"通用视频模型 vs 本方法"），
  只有当主表结果需要"同数据量下范式差异"的补充证据时才补跑（约 3 h/模型）。
- 预训练来源不同（K400/K600/ImageNet）是客观差异：主表用 **K400 同源**，
  另附"从零训练"档作参考（可选，成本翻倍）。
- 统一报告：参数量、训练时长、峰值显存、每视频推理延迟。

---

## 5. 共用数据准备（只做一次，所有模型复用）

### 5.1 密集抽帧

新增 `experiments/prep_p10_frames.py`（复用 `preprocessing/av_decode.py`）：

- 输入：`DATASET-omnifall/data_files/extracted/<class>/<video>.mp4`（AV1，1280×720，81 帧）
- 输出：`data/ref_frames/<class>/<video>/img_00001.jpg ... img_00081.jpg`
- 尺寸：**短边缩放到 256 后中心裁剪 256×256**（模型侧再各自 resize/crop 到 224/160/112）
- 采样：全 81 帧都存，抽样规则交给各模型 dataloader（train 随机、test 均匀）

**成本估算**：972,000 帧；JPEG q=90 约 15–25 KB/帧 → **磁盘 15–25 GB**（Autodl）。
AV1 解码是 CPU 活，PyAV 单进程约 100–200 fps，**16 进程并行约 1–2 小时**。

### 5.2 标注转换

新增 `experiments/prep_p10_annotations.py`，由 `ofsyn_frame_labels.csv` + splits 生成：

- `data/ref_annotations/kinetics/{train,val,test}.csv`：`视频路径 标签`（SlowFast / TimeSformer 用；
  clip 级标签用"窗口中心帧的三元标签"，或按窗口内多数投票）
- `data/ref_annotations/tsm/{train,val}.txt`：`帧目录路径 标签 总帧数`（TSM 用）
- `data/ref_annotations/framewise/{train,val,test}.csv`：逐帧三元标签（滑窗评测用）

### 5.3 评估脚本复用

- 复用 `utils/metrics.py` 的 `compute_ternary_metrics`（instance 口径）与
  `utils/timeline_metrics.py` 的 `compute_video_timeline_metrics` / `aggregate_timeline_metrics`
  （timeline 口径），保证与主线同口径。
- 新增 `experiments/eval_ref_model.py`：读某个模型的逐帧预测 `.npz` → 输出与
  `eval_best.json` 同结构的指标 JSON。

---

## 6. 逐个模型适配方案

### 6.1 TSM（**建议第一个做**，1–2 天）

- **目标配置**：ResNet50 + shift8/blockres，8 帧（对照 16 帧），224×224，三元输出。
- **改造点**：
  1. `ops/dataset_config.py` 新增 `return_omnifall(modality)` → 返回
     `(3, train_list, val_list, root_path=data/ref_frames, prefix='img_{:05d}.jpg')`
  2. 列表文件按 TSM 格式生成（见 5.2）
  3. 训练命令：
     `python main.py omnifall RGB --arch resnet50 --num_segments 8 --shift_div 8 --shift_place blockres --tune_from <TSM_K400_8f.pth> --root_path data/ref_frames --train_list ... --val_list ... --epochs 20 -b 32 --lr 0.005`
- **权重**：`TSM_kinetics_RGB_resnet50_shift8_blockres_avg_segment8_e50.pth`（官方站点直链，约 100 MB）。
- **成本（实测修正两轮，最终值）**：9,600 视频 = 96,000 片段/epoch。
  位移算子的净开销约 **1.7×**（batch8：无位移 42.4 → 官方位移 24.9 clips/s）；
  8 GB 卡上 batch 16 会因显存打满把吞吐压到 7.8 clips/s（**显存压力，非算子问题**），
  故本地应用 batch 8–12，3090 用 batch 32。
  工期：本地 4060 **≈76 min/epoch**（10 epoch ≈ 12–13 h）；3090（外推）**≈18–23 min/epoch**
  （10 epoch ≈ **3–4 h**）。已实现数值等价的快速位移算子（`--fast_shift`，再快 8–15%）。
  详见 `0912实验第十阶段总结.md` §3.4。
- **风险/回退**：TSM 只做通道位移，静态轴（fallen vs lying）预期接近两阶段基线；
  若显著低于主线，正好说明"通用视频模型的时序先验不适用于跌倒因果"。

### 6.2 SlowFast / X3D（2–3 天）

- **目标配置**：先 X3D-XS（4 帧×采样12），再 X3D-S/M；三元输出。
- **改造点**：
  1. 新增 `configs/OmniFall/X3D_XS_omnifall.yaml`：复制 `configs/Kinetics/X3D_XS.yaml`，
     改 `DATA.PATH_TO_DATA_DIR=data/ref_annotations/kinetics`、`MODEL.NUM_CLASSES=3`、
     `TRAIN.BATCH_SIZE=32`、`NUM_GPUS=1`、`SOLVER.MAX_EPOCH=30`。
  2. `slowfast/datasets/kinetics.py` 可直接复用（CSV 就是 `path label`）；若需帧级标签，
     新增 `slowfast/datasets/omnifall.py` 复制 kinetics.py 改列解析。
  3. 运行：`python tools/run_net.py --cfg configs/OmniFall/X3D_XS_omnifall.yaml`
- **权重**：`x3d_xs.pyth / x3d_s.pyth / x3d_m.pyth`（Model Zoo 直链）；换 head 时注意
  `MODEL.NUM_CLASSES` 与 checkpoint 的最后一层不匹配（用 `CHECKPOINT_LOAD_MATCHING`）。
- **成本**：X3D-XS 约 3.8 M 参数、0.6 GFLOPs/帧，8 GB 显存可跑；
  9600 视频 × 30 epoch ≈ **3–6 h**（3090）。X3D-L（18 GFLOPs）另计。
- **风险/回退**：主要坑在环境（detectron2 + fvcore + PyAV/decord）。
  **必须单独建 conda env（建议 py38/py39）**，不要动 `py310`。

### 6.3 TimeSformer（2–3 天）

- **目标配置**：`TimeSformer_divST_8x32_224`（8 帧、224、divided space-time attention）。
- **改造点**：
  1. 复制配置到 `reference/TimeSformer/configs/OmniFall/`，改 `NUM_CLASSES=3`、
     `DATA.PATH_TO_DATA_DIR=data/ref_annotations/kinetics`、`BATCH_SIZE=8`（视显存）。
  2. 加载 K400 预训练（Dropbox `.pyth`），head 随机初始化。
  3. 运行：`python tools/run_net.py --cfg configs/OmniFall/TimeSformer_divST_8x32_224.yaml`。
- **依赖**：`fvcore`、`timm`、`einops`、`yacs`（仓库自带 `timesformer/` 包，
  **训练不需要安装 PySlowFast**，只有可视化脚本需要）。
- **成本**：ViT-B 8×224² batch 8 单卡约 12–16 GB；9600 视频 × 15 epoch ≈ **6–10 h**。
- **风险/回退**：小数据（9600 视频 / 3 类）上 attention 模型极易过拟合 →
  建议冻结 patch embedding、降 lr、强增强；若仍不收敛则退化为 T1 线性探测。
  仓库 2025-01 已归档，旧版 yacs/einops 版本要锁。

### 6.4 I3D（3–5 天，两条路线）

- **路线 A（官方 TF1）**：新建 `conda env tf1`（**Python 3.7 + tensorflow 1.15 + sonnet 1.x**），
  在 Autodl 上跑，输入 64×224×224 RGB。Flow 流需要 TV-L1 光流（972k 帧），
  **本阶段跳过 Flow，只做 RGB**（权重已备：rgb_scratch / rgb_imagenet / rgb_scratch_kin600）。
- **路线 B（PyTorch 移植，推荐）**：按 `i3d.py` 结构在 `models/ref_i3d.py` 复刻
  Inflated Inception-V1，然后写权重转换脚本把 TF checkpoint 映射到 PyTorch
  （关键点：BN moving_mean/var、inflated conv 的时间维复制）。
- **成本**：64 帧 batch 8 约 10–14 GB（3090）；9600 视频 × 20 epoch ≈ **8–15 h**。
- **风险/回退**：TF1 在新驱动/新 GPU 上编译困难；权重转换易踩坑。
  时间紧就只交 T1（用官方 checkpoint 抽 clip 特征 + 线性探测）。

### 6.5 C3D（2–4 天，建议 PyTorch 复刻）

- **为什么不用官方仓库**：Caffe fork 需要 CUDA8 时代工具链，Windows 不可行、
  Linux 现代环境要打大量补丁；且**仓库里没有 .caffemodel 权重**。
- **推荐做法**：按 `conv3d_ucf101_train.prototxt`（crop 112、16 帧、batch 30）在
  `models/ref_c3d.py` 用 PyTorch 复刻（8 个 3D conv + 2 FC，约 78 M 参数）；
  可选：从 C3D 官网下载 Sport1M/UCF101 caffemodel 做权重转换（否则从零训练）。
- **数据**：16 帧、112×112（从 256×256 帧下采样即可）。
- **成本**：16×112² batch 16 约 6–8 GB；9600 视频 × 20 epoch ≈ **3–5 h**。
- **风险/回退**：复刻实现需要在论文中注明"非官方实现、参数量对齐"；
  C3D 时序感受野小、预期是本批最弱基线之一。

### 6.6 ViViT（5–8 天，最后做 / 可降级）

- **官方路线**：JAX/Flax + Scenic，输入需 **DMVR 预处理成 TFRecord**，模型吃 32×224²。
  改造 = 写 TFRecord 生成器 + 数据集配置 + JAX 训练循环，且要新建 JAX 环境。
- **成本**：ViViT-B 32×224² 显存极大，单卡 3090 需 gradient checkpointing，
  batch 4–8；9600 视频训练一轮就要数小时。
- **风险/回退**：本阶段性价比最低，且与 TimeSformer 的结论高度重叠（都是视频 transformer）。
  **建议只做 T1 线性探测**（用官方 K400 checkpoint 抽特征 + 线性头），
  在论文里作为"视频 Transformer 特征质量"的参考点；若时间允许再补 T2。

---

## 7. 执行顺序与里程碑

| 里程碑 | 内容 | 预计 |
|---|---|---|
| M0 | 统一协议落地：抽帧脚本 + 标注生成 + `eval_ref_model.py`（把 clip 预测写回帧级、接 timeline 指标） | 1–2 天 |
| M1 | TSM + X3D-XS 跑通（先 2000 视频冒烟，再 9600 全量） | 3–4 天 |
| M2 | X3D-S/M + TimeSformer | 3–5 天 |
| M3 | I3D（PyTorch 移植 / TF1 环境）+ C3D（PyTorch 复刻） | 5–8 天 |
| M4 | ViViT（线性探测优先，全量微调可选） | 3–8 天 |

每个模型产出一份：`logs/phase10/<model>/{training_history.json, test_results.json, frame_preds.npz}`。

---

## 8. 预期结果与产出

主对比表（论文表 1 的雏形）：

| 模型 | 骨干 | 输入 | 参数量 | 训练数据 | fall_f1 | fallen_f1 | avg_f1 | acc | timeline avg_f1（逐视频） | 训练时长 | 峰值显存 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| E1-full（本项目） | DINOv2-g + LoRA | 10 帧/64 窗口 | 10.8 M(可训) | 9600 | 0.757 | 0.754 | 0.827 | 0.937 | 待补 | ~ | ~ |
| TSM | ResNet50 | 8×224² | 24 M | 9600 | | | | | | | |
| X3D-XS/S/M | X3D | 4-16 帧 | 3.8–6.2 M | 9600 | | | | | | | |
| TimeSformer | ViT-B | 8×224² | 121 M | 9600 | | | | | | | |
| I3D | Inception-V1(3D) | 64×224² | 12 M | 9600 | | | | | | | |
| C3D | C3D | 16×112² | 78 M | 9600 | | | | | | | |
| ViViT-B | ViT-B | 32×224² | 89 M | 9600 | | | | | | | |

**预期结论方向**（待验证）：通用视频模型在 fall（动态）上接近主线，但在
**fallen vs lying（静态+历史）**上普遍更低 —— 正好支撑"跌倒检测需要事件级因果建模"的论点。

---

## 9. 风险与回退

| 风险 | 影响 | 回退方案 |
|---|---|---|
| PySlowFast / TimeSformer 依赖旧、环境难装 | 阻塞 2 个模型 | 独立 conda env（py38/39）；先 T1 线性探测 |
| TF1（I3D）在现代 GPU 上不可用 | 阻塞 I3D | 走 PyTorch 移植；或只交 T1 |
| Caffe（C3D）编译失败 | 阻塞 C3D | PyTorch 复刻 + 从零训练 |
| 抽帧磁盘/耗时超预算 | 阻塞全部 | 先抽 2000 视频子集（与 E1@2000 同批）跑通全流程 |
| 小数据过拟合，基线普遍很差 | 对比不公正 | 统一用 K400 预训练 + 强增强；补"10 类预训练→三元微调"两段式 |
| ViViT 超期 | 拖慢归档 | 降级为线性探测，论文标注 |

---

## 10. 待确认问题

1. 主口径确认：帧级三元 + 滑窗 stride 8 + instance 230,400，是否按此执行？
2. 是否允许在 Autodl 新建独立 conda 环境（py37/py38/py39）用于 I3D / PySlowFast？
3. 每个模型的 GPU 预算上限（小时）大概多少？
4. ViViT 是否必须做全量微调？（建议线性探测即可）
5. 是否需要"从零训练"档作为公平性对照？（成本约翻倍）
6. `reference/`（约 1.5 GB，含权重）是否加入 `.gitignore` 不入库？
