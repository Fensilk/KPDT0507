针对数据集 omnifall 的子集 OF-Syn（OF-Synthetic），我们已经完成了依靠 Resnet18 的数据预处理，形成了三个 npz 文件，分别是data/omnifall_preprocessed.npz、逐帧特征的 data/omnifall_frame_preprocessed.npz 和姿态数据 data/omnifall_pose.npz

已知 RGB+Δclip、RGB+Δclip+pose 有效，根据此方向开展完整消融实验。

研究 RGB、RGB+Δclip、RGB+Δclip、RGB+pose、RGB+Δclip+pose，重点观察fall_f1、fallen_f1、avg_f1 和时间线可视化变化

优化 train_loss 权重，目的是让 fallen 尽量不被 fall 的优化方向带偏

查看 tensorboard，把每个阶段的 acc、loss 记录存储。
